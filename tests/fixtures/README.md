# Test fixtures

⚠️ **Synthetic, pending the sandbox smoke test.** These bodies are hand-built
from the response shapes in the official API docs (apidocs.photoncommerce.com);
they have **not** been captured from the live API yet. All values are
placeholders. When the sandbox smoke test runs, replace them with real,
redacted captures and delete this warning.
