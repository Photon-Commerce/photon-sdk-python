[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "photon-sdk"
version = "0.0.1"
description = "Python SDK for Photon Commerce — process documents via the Photon API."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "Photon Commerce" }]
keywords = ["photon", "photoncommerce", "documents", "ocr", "sdk"]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "ruff>=0.4",
    "mypy>=1.8",
]

[project.urls]
Homepage = "https://photoncommerce.com"
Source = "https://github.com/Photon-Commerce/photon-sdk-python"

[tool.hatch.build.targets.wheel]
packages = ["src/photon"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.mypy]
python_version = "3.9"
strict = true
files = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
